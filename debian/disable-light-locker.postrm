#!/bin/sh

set -e

# Restore light-locker's autostart
if [ "$1" = remove ] ; then
	dpkg-divert --remove --rename --package disable-light-locker --divert /etc/xdg/autostart/light-locker.desktop.distrib /etc/xdg/autostart/light-locker.desktop
fi

#DEBHELPER#

exit 0
