#!/bin/sh

# Remove the short-lived (within zesty dev cycle) /etc/update-motd.d/50-news file,
# which was renamed to /etc/update-motd.d/50-motd-news
rm_conffile /etc/update-motd.d/50-news 9.6ubuntu11~
